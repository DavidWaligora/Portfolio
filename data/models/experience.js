export default class Experience {
    constructor(company, jobTitle, date, description) {
      this.company = company;
      this.jobTitle = jobTitle;
      this.date = date;
      this.description = description;
    }
}